#!/bin/bash

export NETWORK=`zerotier-cli listnetworks | grep -v "<nwid>" | cut -d" " -f3`

while [ -z "$NETWORK" ]
do
  export NETWORK=`zerotier-cli listnetworks | grep -v "<nwid>" | cut -d" " -f3`
  sleep 2
done

echo ${NETWORK}
